class ExampleModel {
  constructor(context) {
    this.name = context.firstName + " " + context.lastName;
  }

  name() {
    return name;
  }
}

export default {
  "some.example.class.path.ExampleModel": ExampleModel
};
